<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Go4Lunch" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/orange</item>
        <item name="colorPrimaryVariant">@color/orange_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/lime</item>
        <item name="colorSecondaryVariant">@color/lime_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="searchViewStyle">@style/Theme.Go4Lunch.SearchView</item>
    </style>

    <!-- Transparent theme for dispatcher activity to avoid loading unnecessary components -->
    <style name="Theme.Go4Lunch.Transparent">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>

    <style name="Theme.Go4Lunch.TranslucentStatusBar">
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Theme.Go4Lunch.SignInButton">
        <item name="android:textAllCaps">false</item>
        <item name="android:textSize">16sp</item>
        <item name="cornerRadius">10dp</item>
        <item name="iconSize">20dp</item>
        <item name="layout_constraintWidth_percent">.8</item>
    </style>

    <!-- ASKME: left icon, background, mic visibility -->
    <style name="Theme.Go4Lunch.SearchView" parent="Widget.AppCompat.SearchView">
        <item name="queryBackground">@color/gray_dark</item>
        <item name="submitBackground">@color/blue_google</item>
        <item name="goIcon">@drawable/ic_favorite_off</item>
        <item name="searchIcon">@drawable/ic_favorite_on</item>
        <item name="commitIcon">@drawable/ic_person</item>
    </style>
</resources>